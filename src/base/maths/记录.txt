

// 使用glam的i32 和 f32 的类型即可


// 最终极简总结
// 95% 图形、镜头、精灵、音频、2D 逻辑统一用 f32（Rust 底层标准）；
// GPU 着色器只认 f32，是硬性底层约束，优先对齐；
// 仅高精度物理 / 超大地图编辑器局部使用 f64，计算后转回 f32；
// 像素下标、纹理尺寸、时间戳用无符号 / 有符号整数 (u32/i32/u64)；
// Python 上层不改动使用习惯，绑定层自动完成 f64 ↔ f32 透明转换，附带精度越界警告。
