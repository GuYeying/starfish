Mipmap 模块待补齐备忘清单（分三类：必修 Bug、PBR 刚需扩展、高阶工业补充）
一、致命 BUG（必须先修，否则现版本不可稳定使用）
管线懒加载缺失：generate 直接 unwrap，未自动创建对应 TextureFormat 的渲染管线，首次调用直接崩溃
无 mip 生成状态标记：Texture 缺少 has_mip_generated: bool，重复生成浪费带宽
UV 未做 0~1 clamp：非 2 次幂纹理 mip 边缘采样溢出，出现黑边
sRGB 双 gamma 错误：sRGB 贴图采样自动转线性，写入 RT 又自动转回 sRGB，画面发灰过曝，缺少 sRGB 专用 shader 分支
Cube/2DArray/3D 纹理创建接口无配套自动生成逻辑，只有 2D 纹理有示例，容易遗漏 mip

二、PBR 基础补齐（推荐实现，覆盖 99% 渲染场景）
新增滤波枚举 MipFilter { BoxLinear, Gaussian3x3 }，TextureDesc 增加 mip_filter 字段
缓存 Key 升级为 (TextureFormat, MipFilter)，缓存高斯管线
编写 3x3 高斯下采样 WGSL，解决粗糙度贴图远距离高光闪烁
统一所有纹理维度（Cube/Array/3D）mip 生成封装，一套 API 兼容全部

三、架构 & 工程健壮性优化
移除全局 panic，增加错误兜底 / Result 返回，资源加载容错
Mipmap 增加多线程安全包装 Arc<Mutex<Self>>，支持多线程资源加载
临时 BindGroup 复用池，避免每层 mip 新建 BG 造成 CPU 开销
合并同纹理多 Layer 为单个 RenderPass，减少通道切换开销
支持自定义指定生成部分 mip 层级，不用强制完整 mip 链

四、高阶扩展（按需延后，开放世界 / 专业管线才需要）
高度图专用加权下采样 shader，解决地形 LOD 台阶锯齿
CPU 预生成 mip 路径，兼容 BC/ASTC 等硬件压缩纹理（GPU Blit 无法处理压缩纹理）
抽象 MipDownsampler trait，支持外部注入自定义下采样管线
支持深度 / 阴影贴图 mip 生成逻辑
可配置滤波模式：Nearest（像素风 UI）、线性、高斯、双边模糊