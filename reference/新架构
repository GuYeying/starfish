Graphics
│
├── RenderContext                     GPU设备上下文（全局唯一）
│     └─ wgpu::Instance
│     └─ wgpu::Adapter
│     └─ wgpu::Device
│     └─ wgpu::Surface

├── RenderQueue                       GPU提交队列
│     └─ wgpu::Queue
│
├── CommandList                       指令录制器（多线程）
│     └─ wgpu::CommandEncoder
│
├── RenderTarget                      渲染目标
│     └─ Vec<GpuTexture>
│     └─ Option<GpuTexture>
│
├── GpuBuffer                         GPU缓冲区
│     └─ wgpu::Buffer
│
├── GpuTexture                        GPU纹理
│     └─ wgpu::Texture
│     └─ wgpu::TextureView
│
├── Sampler                           采样器
│     └─ wgpu::Sampler
│
├── Shader                            WGSL着色器
│     └─ wgpu::ShaderModule
│
├── RenderPipeline                    图形管线
│     └─ wgpu::RenderPipeline
│
├── ComputePipeline                   计算管线
│     └─ wgpu::ComputePipeline
│
├── BindGroup                         Shader资源绑定
│     └─ wgpu::BindGroup
│
├── Mesh                              网格(额外考虑gpu cpu分离)
      ├─ submeshes: Vec<SubMesh>,
│     ├─ vertex_buffer
│     ├─ index_buffer
│     ├─ vertex_count
│     ├─ index_count
│     └─ topology


├── SubMesh
    index_start: u32,
    index_count: u32,


├── Material                          材质(额外考虑gpu cpu分离)
│     ├─ Texture
│     ├─ Sampler
│     ├─ Uniform参数
│     └─ BindGroup缓存
│
├── Camera                            相机(额外考虑gpu cpu分离)
│     ├─ View Matrix
│     ├─ Projection Matrix
│     └─ 纯CPU数据
│
├── Scene
│     ├─ Mesh
│     ├─ Material
│     └─ Camera
└── RenderPass                        内部对象（用户不可见）
      └─ wgpu::RenderPass




Resource
│
Animator 每帧计算：
当前时间
↓
插值关键帧
↓
得到 Bone Matrices

Animation
├─ channels
├─ keyframes
└─ duration
|
├──Bone 
    parent: Option<u32>,
    bind_pose: Mat4,
├──Skeleton
├─ bones
├─ inverse_bind_matrices
└─ hierarchy
├── Mesh
│     ├─ SubMesh[]
│     ├─ Vertex Data
│     ├─ Index Data
│     └─ GPU Cache(可选)
│
├── Material
│     ├─ Texture引用
│     ├─ 参数
│     └─ GPU Cache(可选)
│
└── ShaderAsset


pub struct Mesh {
    submeshes: Vec<SubMesh>,
}
pub struct SubMesh {
    index_start: u32,
    index_count: u32,
}
pub struct Model {
    mesh: Mesh,
    materials: Vec<Material>,
}




#对于实例渲染架构:
    Instancing与下列:
    Skinned Mesh
    Billboard
    LOD
    Batch Rendering
    GPU Culling
    Indirect Rendering :GPU决定画什么
    Particle Rendering
    GPU Skinning
    它们都是：
    Render Technique（渲染提交技术）

#对于延迟着色技术:
    Forward+
    Clustered Forward
    Visibility Buffer
    Nanite风格管线






#进一步架构解析:
    资源层
    ├── Mesh
    ├── Material
    └── Texture

    场景层
    ├── Transform
    ├── MeshRenderer
    └── Light

    提交优化层
    ├── Instancing
    ├── Batching
    ├── LOD
    ├── GPU Culling
    └── Indirect Draw

    渲染管线层
├── Geometry Submission
│   ├── Draw
│   ├── Instancing
│   ├── Batching
│   ├── LOD
│   ├── Frustum Culling
│   ├── Occlusion Culling
│   └── Indirect Draw
│
├── Lighting System
│   ├── Directional Light
│   ├── Point Light
│   ├── Spot Light
│   ├── Area Light
│   ├── Reflection Probe
│   ├── Light Probe
│   ├── IBL
│   └── GI
│
├── Shading Model
│   ├── BlinnPhong
│   ├── PBR Metallic
│   ├── PBR Specular
│   ├── Toon
│   └── Custom
│
└── Rendering Pipeline
    ├── Forward
    ├── Deferred
    ├── Forward+
    ├── Clustered
    ├── Visibility Buffer
    └── GPU Driven